name: docker
description: Docker Model Runner Agent
version: 0.1.0
model:
  provider: docker
  id: ai/smollm2
  port: 12434
role: |
  You are a helpful assistant running locally via Docker Model Runner.
  Provide concise answers.