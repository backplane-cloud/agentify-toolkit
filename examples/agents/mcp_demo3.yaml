name: mcp_demo3
description: Demo Agent for MCP Server
version: 0.1.0
model:
  provider: xai
  id: grok-4
  api_key_env: XAI_API_KEY
role: The agent can retrieve the weather in Denmark
tools:
  - random_user
  - add_numbers
  - user_api
mcp:
  servers:
    - name: utilities
      endpoint: http://localhost:3333/mcp
